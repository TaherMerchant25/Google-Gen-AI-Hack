import React from 'react'
import ReactDOM from 'react-dom/client'
import NewsRetriever from './1.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NewsRetriever />
  </React.StrictMode>,
)
